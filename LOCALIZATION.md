# Система локализации (i18n) - Gitr Social Network

## Обзор

Этот проект включает полностью реализованную систему многоязычности (i18n) для социальной сети Gitr с поддержкой русского и английского языков.

## Структура проекта

```
/
├── locales/
│   ├── ru.php          # Русские переводы
│   └── en.php          # Английские переводы
├── src/
│   ├── Localization.php # Класс локализации
│   └── helpers.php      # Вспомогательные функции
├── public/
│   ├── index.php        # Главная страница
│   ├── auth.php         # Страница входа/регистрации
│   ├── profile.php      # Профиль пользователя
│   ├── messages.php     # Сообщения
│   ├── settings.php     # Настройки
│   └── _api/
│       ├── language.php # API для переключения языка
│       └── auth.php     # API регистрации/входа
└── LOCALIZATION.md      # Эта документация
```

## Основные компоненты

### 1. Класс Localization (src/Localization.php)

Основной класс, управляющий локализацией приложения.

**Основные методы:**

- `setLanguage(string $language)` - Установить язык
- `getLanguage()` - Получить текущий язык
- `translate(string $key, array $replacements = [])` - Получить перевод
- `getSupportedLanguages()` - Получить список поддерживаемых языков
- `has(string $key)` - Проверить наличие перевода

**Пример использования:**

```php
use App\Localization;

$localization = new Localization();
$localization->setLanguage('ru');

echo $localization->translate('auth.login'); // Вывод: "Вход"
```

### 2. Вспомогательные функции (src/helpers.php)

Набор удобных функций для работы с локализацией.

**Основные функции:**

```php
// Получить перевод
trans('auth.login'); // "Вход" (русский) или "Login" (английский)

// Получить текущий язык
get_language(); // "ru" или "en"

// Установить язык
set_language('en');

// Получить поддерживаемые языки
get_supported_languages(); // ['ru', 'en']

// Проверить поддержку языка
is_language_supported('ru'); // true

// Получить флаг языка
get_language_flag('ru'); // "🇷🇺"

// Получить имя языка
get_language_name('ru'); // "Русский"
```

### 3. Файлы переводов

Переводы хранятся в файлах `/locales/[язык].php` и возвращают ассоциативный массив.

**Структура перевода:**

```php
return [
    'auth' => [
        'login' => 'Вход',
        'register' => 'Регистрация',
        ...
    ],
    'profile' => [
        'profile' => 'Профиль',
        'edit_profile' => 'Редактировать профиль',
        ...
    ],
    ...
];
```

**Доступные секции:**

- `auth` - Аутентификация (вход, регистрация)
- `profile` - Профиль пользователя
- `feed` - Лента постов
- `posts` - Посты
- `comments` - Комментарии
- `likes` - Отметки "нравится"
- `errors` - Сообщения об ошибках
- `nav` - Навигация
- `messages` - Сообщения
- `settings` - Настройки
- `common` - Общие элементы
- `search` - Поиск
- `notifications` - Уведомления

## Использование в приложении

### Инициализация

В начале каждого PHP файла подключите систему локализации:

```php
<?php
session_start();

require_once dirname(__DIR__) . '/src/Localization.php';
require_once dirname(__DIR__) . '/src/helpers.php';
?>
```

### В HTML шаблонах

```html
<html lang="<?php echo get_language(); ?>">
    <head>
        <title><?php echo trans('feed.home'); ?> - Gitr</title>
    </head>
    <body>
        <h1><?php echo trans('auth.login'); ?></h1>
        <form>
            <label><?php echo trans('auth.email'); ?></label>
            <input type="email">
            
            <label><?php echo trans('auth.password'); ?></label>
            <input type="password">
            
            <button><?php echo trans('auth.sign_in'); ?></button>
        </form>
    </body>
</html>
```

### Замены в переводах

Переводы поддерживают замены переменных:

**В файле перевода:**

```php
'likes' => [
    'likes_count' => 'Нравится :count',
]
```

**В коде:**

```php
trans('likes.likes_count', ['count' => 42])
// Результат: "Нравится 42"
```

### Переключатель языка

На каждой странице есть переключатель языка:

```html
<div class="language-switcher">
    <?php foreach (get_supported_languages() as $lang): ?>
        <a href="?lang=<?php echo $lang; ?>" class="<?php echo get_language() === $lang ? 'active' : ''; ?>">
            <?php echo get_language_flag($lang); ?> <?php echo strtoupper($lang); ?>
        </a>
    <?php endforeach; ?>
</div>
```

JavaScript обработчик:

```javascript
const urlParams = new URLSearchParams(window.location.search);
const lang = urlParams.get('lang');
if (lang) {
    fetch('/_api/language.php?action=set&language=' + lang)
        .then(response => response.json())
        .catch(error => console.error('Error:', error));
}
```

## Сохранение выбора языка

Язык сохраняется в следующем порядке приоритета:

1. **Сессия** - `$_SESSION['user_language']`
2. **Cookie** - `language` (сохраняется на 30 дней)
3. **Заголовок Accept-Language** - автоматическое определение по браузеру
4. **Язык по умолчанию** - английский (`en`)

## API эндпоинты

### Изменение языка

**Запрос:**
```
GET /_api/language.php?action=set&language=ru
```

**Ответ:**
```json
{
    "success": true,
    "message": "Success",
    "data": {
        "language": "ru",
        "flag": "🇷🇺"
    }
}
```

### Получение информации о языке

**Запрос:**
```
GET /_api/language.php?action=get
```

**Ответ:**
```json
{
    "success": true,
    "message": "Success",
    "data": {
        "language": "ru",
        "supported_languages": ["ru", "en"]
    }
}
```

### Регистрация пользователя с локализацией ошибок

**Запрос:**
```
POST /_api/auth.php
Content-Type: application/x-www-form-urlencoded

action=register&username=john&email=john@example.com&password=password123&confirm_password=password123
```

**Успешный ответ:**
```json
{
    "success": true,
    "message": "Sign Up",
    "data": {
        "user_id": 1234,
        "username": "john",
        "email": "john@example.com"
    }
}
```

**Ошибка валидации:**
```json
{
    "success": false,
    "message": "Validation failed",
    "errors": {
        "email": "Invalid email format",
        "password": "Password must be at least 6 characters"
    }
}
```

## Локализованные страницы

Проект включает следующие примеры локализованных страниц:

### 1. Главная страница (`/index.php`)
- Лента постов
- Поле для создания поста
- Боковая панель с уведомлениями
- Полностью локализована

### 2. Страница входа/регистрации (`/auth.php`)
- Переключение между входом и регистрацией
- Валидация форм с локализованными ошибками
- Чувствительна к выбору языка

### 3. Профиль пользователя (`/profile.php`)
- Информация о профиле
- Форма редактирования
- Статистика подписчиков

### 4. Сообщения (`/messages.php`)
- Список диалогов
- Чат-окно с сообщениями
- Полностью локализовано

### 5. Настройки (`/settings.php`)
- Выбор языка с красивым интерфейсом
- Выбор темы
- Смена пароля
- Удаление аккаунта

## Добавление новых языков

Для добавления нового языка:

1. **Создайте файл перевода:**
   ```
   /locales/[код_языка].php
   ```

2. **Добавьте язык в список поддерживаемых:**
   Отредактируйте `src/Localization.php`, измените константу `SUPPORTED_LANGUAGES`:
   ```php
   private const SUPPORTED_LANGUAGES = ['ru', 'en', 'de', 'fr'];
   ```

3. **Добавьте флаг языка в helpers.php:**
   ```php
   $flags = [
       'ru' => '🇷🇺',
       'en' => '🇬🇧',
       'de' => '🇩🇪',
   ];
   ```

4. **Добавьте имя языка в helpers.php:**
   ```php
   $names = [
       'ru' => 'Русский',
       'en' => 'English',
       'de' => 'Deutsch',
   ];
   ```

## Лучшие практики

1. **Используйте вспомогательные функции** - Используйте `trans()` вместо прямого обращения к классу
2. **Структурируйте переводы** - Организуйте переводы по логическим секциям
3. **Используйте точечную нотацию** - `trans('auth.login')` вместо доступа к массиву
4. **Локализуйте все текстовые строки** - Включая сообщения об ошибках и уведомления
5. **Тестируйте оба языка** - Убедитесь, что длинные переводы не ломают макет
6. **Используйте placeholders для переменных** - `:variable` вместо конкатенации строк

## Проверка работоспособности

Чтобы проверить систему локализации:

1. Откройте `http://localhost/public/index.php`
2. Нажмите на флаги языков в правом верхнем углу
3. Убедитесь, что интерфейс переключается между русским и английским
4. Проверьте сохранение выбора языка при перезагрузке страницы
5. Посетите разные страницы и убедитесь, что язык сохраняется

## Возможности расширения

- Добавить поддержку плюрализации (один/много элементов)
- Добавить форматирование дат и валют в зависимости от языка
- Реализовать загрузку переводов из БД вместо файлов
- Добавить кэширование переводов для лучшей производительности
- Реализовать автоматический синтаксис для RTL языков (арабский, иврит)

## Заключение

Эта система локализации обеспечивает полную поддержку многоязычности для социальной сети Gitr. Она легко расширяется, хорошо структурирована и следует лучшим практикам веб-разработки.

# Gitr - Социальная сеть

Полнофункциональная социальная сеть с возможностями создания профилей, постов, лайков и комментариев.

## Возможности

### 👤 Профили пользователей
- Расширенные профили с биографией и аватарами
- Счетчики подписчиков и подписок
- Статистика пользователя (посты, лайки, комментарии)

### 📝 Посты и контент
- Создание и редактирование постов
- Лента новостей с пагинацией
- Просмотр отдельных постов

### ❤️ Социальные функции
- Лайки и дизлайки постов
- Комментарии к постам
- Отображение количества лайков и комментариев

### 🔐 Безопасность
- Аутентификация пользователей
- Защита от несанкционированного доступа
- Валидация входных данных

## Технический стек

- **Backend**: PHP 7.4+
- **База данных**: MySQL 5.7+
- **Frontend**: HTML5, CSS3, Vanilla JavaScript
- **Архитектура**: MVC с простой структурой классов

## Структура проекта

```
/home/engine/project/
├── config/
│   └── database.php          # Конфигурация БД
├── database/
│   └── migrations.sql        # Миграции БД
├── src/
│   ├── Auth.php              # Аутентификация
│   ├── Database.php          # Подключение к БД
│   ├── Post.php              # Управление постами
│   ├── Comment.php           # Управление комментариями
│   ├── Like.php              # Управление лайками
│   └── Profile.php           # Управление профилями
├── public/
│   ├── api/                  # API endpoints
│   │   ├── auth.php
│   │   ├── posts.php
│   │   ├── feed.php
│   │   ├── like.php
│   │   └── comment.php
│   ├── css/
│   │   └── style.css         # Стили
│   ├── js/
│   │   └── app.js            # JavaScript
│   ├── index.php             # Главная страница
│   └── login.php             # Вход
├── pages/
│   ├── profile.php           # Профиль пользователя
│   ├── feed.php              # Лента постов
│   └── post/
│       ├── create.php        # Создание поста
│       └── view.php          # Просмотр поста
├── views/
│   └── index.php             # Домашняя страница
└── .htaccess                 # Конфигурация сервера
```

## Установка

### 1. Требования
- PHP 7.4 или выше
- MySQL 5.7 или выше
- Веб-сервер (Apache/Nginx)

### 2. Настройка базы данных

1. Создайте базу данных MySQL:
```sql
CREATE DATABASE gitr CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

2. Импортируйте миграции:
```bash
mysql -u username -p gitr < database/migrations.sql
```

### 3. Настройка конфигурации

Создайте файл `.env` в корне проекта:
```env
DB_HOST=localhost
DB_NAME=gitr
DB_USER=your_username
DB_PASS=your_password
```

### 4. Настройка веб-сервера

#### Apache
Убедитесь, что mod_rewrite включен и настройте DocumentRoot на папку `/home/engine/project/public`.

#### Nginx
```nginx
server {
    listen 80;
    server_name your_domain.com;
    root /home/engine/project/public;
    index index.php;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        fastcgi_pass unix:/var/run/php/php7.4-fpm.sock;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        include fastcgi_params;
    }
}
```

### 5. Настройка прав доступа

```bash
chmod -R 755 /home/engine/project
chmod -R 777 /home/engine/project/tmp
chown -R www-data:www-data /home/engine/project
```

## Использование

### Демо аккаунты

Для тестирования доступны следующие аккаунты:

| Email | Пароль | Описание |
|-------|--------|----------|
| admin@gitr.com | password | Администратор |
| john@example.com | password | Разработчик |
| jane@example.com | password | Дизайнер |

### Основные функции

#### 1. Аутентификация
- Вход в систему через `/login.php`
- Выход из системы
- Проверка статуса аутентификации

#### 2. Профиль пользователя
- Просмотр профиля: `/profile.php`
- Редактирование профиля (в разработке)
- Статистика пользователя

#### 3. Посты
- Создание постов: `/post/create.php`
- Просмотр постов: `/post/view.php?id={post_id}`
- Лента новостей: `/feed.php`

#### 4. Социальные функции
- Лайки постов (API: `POST /api/like`)
- Комментирование постов (API: `POST /api/comment`)
- Просмотр комментариев

### API Endpoints

#### Аутентификация
- `GET /api/auth.php` - проверка статуса аутентификации
- `POST /api/auth.php` - вход в систему
- `DELETE /api/auth.php` - выход из системы

#### Посты
- `POST /api/posts.php` - создание поста
- `GET /api/feed.php` - получение ленты новостей

#### Взаимодействие
- `POST /api/like.php` - лайк/анлайк поста
- `POST /api/comment.php` - добавление комментария

## Разработка

### Добавление новых функций

1. **Новый класс модели**: Создайте файл в `/src/`
2. **API endpoint**: Добавьте файл в `/public/api/`
3. **Страница**: Добавьте файл в `/pages/`
4. **Стили**: Добавьте CSS в `/public/css/style.css`

### Тестирование

```bash
# Запуск PHP built-in сервера (для разработки)
php -S localhost:8000 -t public

# Подключение к БД
mysql -u username -p gitr
```

### Отладка

1. Включите отображение ошибок в PHP:
```php
ini_set('display_errors', 1);
ini_set('display_startup_errors', 1);
error_reporting(E_ALL);
```

2. Проверьте логи веб-сервера
3. Используйте инструменты разработчика браузера

## Безопасность

- Все пользовательские входные данные валидируются
- SQL-инъекции предотвращаются через Prepared Statements
- XSS атаки предотвращаются через htmlspecialchars()
- CSRF защита (базовая)

## Производительность

- Пагинация для больших списков постов
- Оптимизированные запросы к БД
- Минификация CSS/JS (рекомендуется)
- Кэширование статических ресурсов

## Поддержка

Для вопросов и предложений создавайте issue в репозитории проекта.

## Лицензия

MIT License - см. файл LICENSE для подробностей.